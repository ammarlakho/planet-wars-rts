package games.planetwars.core

class ForwardModel (val state: GameState){
    // the forward model applies the current set of actions to the current game state
    // and updates the game state in place




}